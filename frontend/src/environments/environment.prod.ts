export const environment = {
  production: true,
  apiUrl: 'https://your-backend-domain.com/api', // Update this with your actual backend URL
  // For now, you can use a placeholder or disable API calls during build
  // apiUrl: '', // This will prevent API calls during build
};
